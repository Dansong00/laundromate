{
  "name": "@laundromate/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for LaundroMate",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "test": "echo 'No tests configured yet' && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
